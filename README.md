```
abeerance@somewhere:~$ cat /dev/me | head -42
```

Frontend, backend, DevOps when nobody's looking. TypeScript by day, Rust when I'm feeling like the compiler should fight me back.

**the stack of the moment**

```ts
const tools = {
  langs: ["TypeScript", "Rust"],
  ui:    ["React 19", "Next.js", "TanStack Start", "Tailwind", "GSAP"],
  api:   ["axum", "Hono", "Better Auth"],
  data:  ["Postgres", "Drizzle", "sqlx"],
  vibes: ["Biome", "oxlint", "fish", "Zen"],
  pets:  "self-hosting aficionado",
};
```

**about the GitHub graph**

Empty-ish? Yeah. Most of my code lives on a self-hosted git platform that you don't have credentials for. This profile mostly hosts things that need to be visible to the public internet, which is a smaller list than my therapist would prefer.

If a tool makes me sign up to see my own live logs, it gets `apt purge`d the same day. The bar is low. It is also hardcoded.

**things I will defend in a heated debate**

- Tabs are correct. Spaces are also correct. Fight me about either.
- A `console.log` left in prod is character development.
- Yak shaving is a renewable resource.
- The browser back button is a UX failure mode you should test for.
- Self-hosting is cheaper than therapy, technically.

**reach me**

- carrier pigeon: deprecated since v0.1.0

`$ uptime: probably fine`
